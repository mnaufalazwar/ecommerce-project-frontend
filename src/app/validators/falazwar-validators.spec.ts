import { FalazwarValidators } from './falazwar-validators';

describe('FalazwarValidators', () => {
  it('should create an instance', () => {
    expect(new FalazwarValidators()).toBeTruthy();
  });
});
